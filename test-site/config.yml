site:
  name: My Site
  url: https://example.com
  language: en

paths:
  template: template
  source: source/public
  dist: dist
  cache: .cache

build:
  clean: true
  pretty_urls: true
  strict_selectors: false

blog:
  posts_dir: blog
  index_layout: page
  index_slug: blog
  posts_per_page: 10
